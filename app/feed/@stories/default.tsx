/**
 * @pattern Parallel Routes — default.tsx (Fallback)
 * @description
 * @stories 슬롯의 default fallback.
 */
export default function StoriesDefault() {
  return (
    <div className="rounded-lg border border-border p-4 text-center text-sm text-text-muted">
      @stories 슬롯 기본 상태
    </div>
  )
}
